
  // seconds = seconds < 1 ? '' : seconds  == 1 ? seconds + ' second' : 'seconds';
  // minutes = minutes < 1 ? '' : minutes  == 1 ? minutes + ' minute' : 'minutes';
  // hours = hours < 1 ? '' : hours  == 1 ? hours + ' hour' : 'hours';
  // days = days < 1 ? '' : days  == 1 ? days + ' day' : 'days'; 
  // years = years < 1 ? '' : years  == 1 ? years + ' year' : 'years'; 
